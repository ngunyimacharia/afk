## Agent skills

### Issue tracker

Issues are tracked as local markdown files under `.scratch/`. Use `artifacts/opencode/commands/to-scratch.md` for scratch package conventions.

### Triage labels

Triage uses the canonical status strings documented in `artifacts/opencode/commands/to-scratch.md`.
